enum UserType {
  CUSTOMER
  STAFF
}

enum AuthProvider {
  LOCAL
  GOOGLE
}

enum UserStatus {
  PENDING_VERIFICATION
  ACTIVE
  SUSPENDED
}

enum LoginAuthMethod {
  LOCAL
  GOOGLE
  REFRESH
}

enum LoginResult {
  SUCCESS
  FAILED
}
