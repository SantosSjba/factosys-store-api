async function main(): Promise<void> {
  // Seed base placeholder. Real seeding logic will be added with Prisma models.
}

main().catch((error: unknown) => {
  // eslint-disable-next-line no-console
  console.error(error);
  process.exit(1);
});
